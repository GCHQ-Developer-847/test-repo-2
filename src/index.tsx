import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    {/* The application will be rendered here once components are added */}
  </React.StrictMode>,
  document.getElementById('root')
);